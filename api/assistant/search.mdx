---
title: "Search documentation"
openapi: "/discovery-openapi.json POST /v1/search/{domain}"
keywords: [ "search", "query" ]

---

## Rate limits

This endpoint shares its rate limits with the other assistant API endpoints. Requests against any combination of assistant endpoints count toward the same limits:

- 10,000 requests per Mintlify organization per hour
- 10,000 requests per IP per day
